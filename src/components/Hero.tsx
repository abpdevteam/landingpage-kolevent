import React from 'react';

const Hero: React.FC = () => {
  return (
      <section className="relative min-h-[50vh] flex items-center justify-center overflow-hidden bg-gray-200">
        <p className={"justify-center"}>Art work</p>
      </section>
  );
};

export default Hero;
